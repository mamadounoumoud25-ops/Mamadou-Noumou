databases:
  - name: ujad-pg
    plan: free

services:
  - type: web
    name: ujad-app
    env: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.12.2
      - key: JWT_SECRET
        sync: false
      - key: RECOVERY_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: ujad-pg
          property: connectionString
